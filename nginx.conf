server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # long-cache hashed assets
  location /assets/ {
    expires 1y;
    add_header Cache-Control "public, immutable";
    try_files $uri =404;
  }

  # product shots, hero clips, posters, brand icons and fonts — served from origin
  location ~ ^/(media|icons|fonts)/ {
    expires 30d;
    add_header Cache-Control "public";
    try_files $uri =404;
  }

  # SPA fallback. index.html must revalidate on every load or browsers keep
  # serving stale asset hashes after a deploy.
  location / {
    add_header Cache-Control "no-cache";
    try_files $uri $uri/ /index.html;
  }

  gzip on;
  gzip_types text/css application/javascript image/svg+xml application/json;
  gzip_min_length 1024;
}
